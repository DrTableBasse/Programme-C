#include <math.h>
#include <stdio.h>
#include <stdlib.h>
#include <time.h>

#include <complex.h>
#include "./plots/pbPlots.h"
#include "./plots/supportLib.h"



// Déclarez la fonction pour afficher le menu et demander à l'utilisateur de choisir une option

float equation_2nd_degres(void);

float suiteRn(void);

void fibonacci();

int goldFibo();

unsigned long calculfibo(int n );


//partie dédié au programme 421

int lancer_de();
extern bool in(short value, short *array, short size);
extern bool all_in(short *list, short *array, short size);
extern short roll_dice();
extern void display_gotten_numbers(short *list, int size);
extern void keep(short list[], short size);
void quatredeuxun();